function login() {
  var t = document.getElementsByName("email")[0].value;
  document.getElementsByName("password")[0].value;
  t.startsWith("S")
    ? (window.location.href = "/student")
    : t.startsWith("F")
    ? (window.location.href = "/faculty")
    : alert("Invalid login credentials");
}
